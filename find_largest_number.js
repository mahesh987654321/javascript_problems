// using function from an array

// function add(number) {
//   let largest = 0;
//   for (let i = 0; i < number.length; i++) {
//     let element = number[i];
//     if (element > largest) {
//       largest = element;
//     }
//   }
//   return largest;
// }
// const num = [1, 2, 3, 34, 54, 6, 7, 7, 56654, 54, 4, 4, 5];
// const amn = add(num);
// console.log(amn);

// find the largest number using if condition

/* function largest(f, s, t) {
  if (f < s && f < t) {
    return f, "First Is largest number1", f;
  } else if (s < f && s < t) {
    return s, "Second is largest number2", s;
  } else {
    return t, "Third is largest number3", t;
  }
}
console.log("Smallest number is ", largest(56, 1, 4)); */
